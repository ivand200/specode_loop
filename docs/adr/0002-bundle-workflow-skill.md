# Bundle the Workflow Skill

Specode Loop will ship its own versioned workflow skills in the repository and copy those bundled workflow skills into each target project before sandbox execution. This makes clone-and-run usage deterministic for users who do not have local Codex skills installed.
