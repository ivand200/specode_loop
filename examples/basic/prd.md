# Example PRD: Specode Loop Demo

## Goal

Demonstrate the project shape expected by Specode Loop.

## Desired Behavior

The sandboxed agent should complete small local file tasks from `plan.md`, one task per sandbox iteration.

## Constraints

- Keep all changes inside this example project.
- Do not make a Git commit.
- Keep output deterministic and plain text.
