[project]
name = "specode-loop"
version = "0.1.0"
description = "Specode Loop runner and regression tests"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]

